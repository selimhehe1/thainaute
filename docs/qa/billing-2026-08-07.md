# QA facturation — 7 août 2026

## Portée

Socle serveur et interface de compte uniquement. Aucun produit Stripe, client
distant, projet RevenueCat, achat, webhook hébergé ou encaissement réel n'a été
créé.

## Preuves locales

- `pnpm --filter @thainaute/web exec vitest run tests/billing-runtime.test.ts tests/billing-service.test.ts tests/billing-http.test.ts tests/billing-section.test.tsx tests/account-experience.test.tsx` : 29 tests passés lors de la tranche ciblée ; la suite web complète est passée avec 572 tests.
- `pnpm test` : 1 294 tests passés dans les 7 workspaces (35 analytics, 76 contenu, 8 tokens, 11 domaine, 219 sync, 373 mobile, 572 web).
- `pnpm db:test` : 8 fichiers, 245 assertions pgTAP passées.
- `pnpm db:lint` : aucune alerte sur la migration billing ; seul l'avertissement historique de `commit_attempt_batch_v1_legacy` subsiste.
- Advisors sécurité Supabase : aucun finding ; l'advisor performance ne remonte que des informations sur des index et une clé étrangère existants.
- `pnpm build -- --no-cache` : build Next.js, exports Expo Android/iOS et routes billing compilés.
- `pnpm test:e2e:web` : 15 passés, 3 ignorés parce que le mode Supabase connecté n'était pas activé dans ce processus.
- `pnpm test:e2e:mobile` : 1 flow Maestro passé sur l'émulateur Android.

## Limites

Les achats natifs nécessitent encore les configurations Apple/Google et
RevenueCat. Le mode reste `disabled` par défaut ; `stripe_test` exige une
sandbox explicitement configurée. Les prix, la fiscalité, les règles de
boutique et le go/no-go live restent ouverts.

## Durcissement P0 vérifié le 8 août 2026

- `checkout.session.completed` n'accorde plus Premium. Les statuts Stripe
  `incomplete`, `incomplete_expired`, `past_due`, `unpaid`, le premier paiement
  échoué et l'action de paiement requise sont révoqués par défaut.
- Le `livemode` Stripe doit correspondre au mode attendu. RevenueCat exige
  désormais l'autorisation statique, le HMAC du corps brut, un timestamp dans
  une fenêtre de cinq minutes, l'environnement attendu et un App ID autorisé.
- `pnpm --filter @thainaute/web exec vitest run tests/billing-runtime.test.ts tests/billing-service.test.ts tests/billing-http.test.ts --reporter=verbose` :
  33 tests passés.
- Suite élargie avec panneau billing et readiness : 63 tests passés dans six
  fichiers.
- `pnpm --filter @thainaute/web test` : 80 fichiers et 591 tests passés après
  intégration des changements P0 parallèles.
- Aucune migration, dépendance, activation fournisseur, ressource distante ou
  variable réelle n'a été créée.

Le miroir reste impropre à une activation multi-fournisseur : sa clé actuelle
est `(user_id, entitlement)` et non `(user_id, entitlement, provider,
subscription)`. Une union Stripe/RevenueCat sûre, la suppression coordonnée du
compte et une recette sandbox réelle restent des bloqueurs explicites.
